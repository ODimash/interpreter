# Определение функции
fnc Greet() {
    write("Hello, world!")
}

# Вызов функции
Greet()

# Цикл для вывода чисел от 1 до 5
loop(5) {
    write(i)
}

# Условная конструкция
if (i > 3) {
    write("i is greater than 3")
} else if (i = 3) {
    write("i is less than or equal to 3")
}

# Присваивание переменной
var x := 10

# Неправильные коды
var 1var := 1
var var2 := 1.2.2
var := $121